name: Release zj-loop-init

on:
  push:
    tags:
      - 'zj-loop-init-v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to publish (e.g. zj-loop-init-v0.1.0)'
        required: true
        type: string

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && format('refs/tags/{0}', github.event.inputs.tag) || github.ref }}

      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'
          cache: 'npm'
          cache-dependency-path: tools/zj-loop-init/package-lock.json

      - name: Ensure npm supports provenance publishing
        run: npm install -g npm@latest

      - name: Test, build & publish
        working-directory: tools/zj-loop-init
        run: |
          npm ci
          npm test
          npm publish --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
