<!-- ROADMAP_SECTION_START -->
## ZJ Roadmap

> 数据文件: `opn-real-agent-dogfood-next-milestone-roadmap.json` | 最后更新: 2026-08-03 12:06:55

[~][X+] 1. OPN Real Agent Dogfood 下一里程碑
├── [x][X+] 1-1. Provider-neutral real-agent-dogfood contract
├── [x][Y+] 1-2. Deterministic Single-Agent OPN Atom conformance fixture
├── [x][X+] 1-3. Provider-neutral real-agent-dogfood CLI entry
├── [~][Y+] 1-4. Opt-in real Agent read-only execution
│   ├── [x][Y+] 1-4-1. Provider-neutral bootstrap protocol and memory failure-matrix fixture
│   ├── [ ][Y+] 1-4-2. macOS process identity PID and Unix socket verification integration
│   └── [ ][Y+] 1-4-3. TrustedRunner Runtime sidecar FD bootstrap and execution process-group orchestration
├── [ ][Y+] 1-5. Dogfood Evidence and independent verification chain
└── [ ][Y+] 1-6. Human review and acceptance handoff

### 当前施工：1-4. Opt-in real Agent read-only execution

**决策：**
- Q: 问题4：真实 Agent 是否必须在专用 isolated worktree 中执行，且执行前后 worktree 都必须 clean？ → 同意 (执行前已有 dirty 状态直接 blocked；执行后出现未授权修改也 blocked；只接受可验证的 worktree 状态，不依赖 prompt 或 Agent 自报。)
- Q: 问题10：Provider/环境/verifier 失败或 Human reject/request-revision 后，是否必须创建新的 execution attempt，旧 attempt 保持 append-only，不允许原地重试或覆盖？ → 同意 (新 attempt 必须重新执行 preflight、TrustedRunner proof、Human approval、Agent execution、verification 和 Human review；旧失败/修订事实不可变。)
- Q: 问题18：首个真实 dogfood 是否复用现有 Codex invocation（exec --json --ephemeral --sandbox read-only --ask-for-approval never --cd isolated-worktree），不增加写/网络/持久化权限，并由外层 Human approval + TrustedRunner 统一治理？ → 同意 (Codex 只是首个 Provider；具体 invocation 服从 OPN 通用 execution contract，不能自行扩大权限；--ask-for-approval never 不替代外层 Human approval。)
- Q: 问题19：首个真实 dogfood 是否只在当前已验证的 macOS TrustedRunner 环境运行；Linux/Windows 或无法证明 Seatbelt/network deny 的环境直接 blocked，不伪造跨平台通过？ → 同意 (Provider-neutral contract 可以跨平台存在，但 native environment proof 按平台分别实现；当前只接受已验证的 macOS proof，其他环境 blocked。)
- Q: 问题20：首个真实 dogfood 是否禁止继承 Provider 登录态、API token、Keychain credential 或其他本机凭据；Provider 需要凭据才能启动时直接 blocked，credential broker 另开范围？ → 同意 (TrustedRunner signer 不等于 Provider credential；env allowlist、文件权限和 HumanSigner 都不能被用来绕过凭据边界；需要登录态时不降低证明标准。)
- Q: 问题21：首个真实 dogfood 是否要求 Provider 在 network-denied 环境中仍能完成任务；Provider 需要远程模型/网络时直接 blocked，不把通常不联网当作证明？ → 同意 (首个真实 Provider 必须具备本地/offline 可执行路径，或至少能在当前 network-denied TrustedRunner 中完成真实任务；不能通过放宽网络 gate 来迁就 Provider。)
- Q: 问题21修正：network-denied 是否不再是所有真实 Coding Agent 的硬性前置条件，而改为 network-denied 或 network-allowed-with-explicit-policy 两种显式策略分支？ → 同意 (主流 Coding Agent 可能依赖远程模型；允许显式 network-allowed-with-explicit-policy，但必须绑定 Human approval、网络范围/目的、policy digest、execution/attempt 和 TrustedRunner 观测。通常不联网、prompt 约束或 Agent 自报不能替代网络策略证明。)
- Q: 问题24：是否采用 coarse network policy，仅验证本次 execution 使用 network-denied 或 network-allowed 且无策略漂移，endpoint 级网络治理另开路线图？ → 是 (当前优先把 OPN 产品做到可用、好用；暂不做 Provider endpoint allowlist、域名/IP 精细限制、Provider 与 Agent 网络拆分、DNS/代理/TLS destination 级审计；这些属于后续网络治理范围。)
- Q: 问题184：1-4 的首个真实验收是否应采用真实 Agent + network-allowed 粗粒度策略作为主路径，以支持依赖远程模型的 Coding Agent；同时要求 macOS TrustedRunner 提供真实 Seatbelt/environment/process/worktree proof，network-denied 作为独立验证分支，而不是主路径硬前置？ → 同意 (主路径优先验证真实可用的 OPN 体验；network-allowed 仍必须经过 Human approval、macOS TrustedRunner proof、execution binding 和 bounded observation。network-denied 保留为独立安全验证分支，endpoint 级网络治理后置。)
- Q: 问题185：1-4 是否应先实现明确的 macOS TrustedRunner 安装/初始化流程：由 Human 显式编译或安装 native helper，读取其不可导出 P-256 公钥指纹，经 Human-signed registry enrollment 注册后才能执行；禁止测试脚本临时生成 helper、自动注册或使用未登记的 helper？ → 同意 (安装/初始化负责产生可审计的 helper 身份与 registry enrollment；执行阶段只消费已登记、未撤销且 digest 一致的 helper。测试 fixture 不得替代真实安装和 enrollment。)
- Q: 问题186：TrustedRunner 初始化是否应拆成 Install/verify 与 Enroll 两个显式阶段；Install/verify 只验证本机 helper，Enroll 才由 Human 选择 network、runner identity 和 capabilities 并签名写入网络级 StateStore；未 enrollment 的 helper 只能是 installed-unenrolled，不能执行？ → 同意 (安装验证与网络授权分离；本机安装不自动加入任何 network，只有 Human 明确发起 enrollment 并完成 registry mutation 后，helper 才具备 execution admission 资格。)
- Q: 问题187：TrustedRunner enrollment 的 capabilities 是否采用“helper 证明上限 + Human 选择子集”模型；Install/verify 产生可证明的最大能力集合，Human 只能注册其子集，不能授予未证明能力，execution admission 只能请求已注册子集？ → 同意 (能力声明与授权分离：helper/platform proof 决定可证明上限，Human enrollment 决定网络内实际授权子集，StateStore 记录最终批准集合。)
- Q: 问题188：TrustedRunner registry enrollment 是否必须绑定 helper 的不可变身份元数据（runner_id、platform、helper_version、helper_digest、public_key_fingerprint、capabilities、status），并要求 execution admission 校验当前 helper 与 registry snapshot 全部一致；任一漂移直接 blocked？ → 同意 (registry 登记的是可执行 helper 身份而不只是公钥；helper 替换、重编译、平台或版本漂移都必须重新 install/verify 与 enrollment，旧 execution 不自动迁移。)
- Q: 问题189：macOS TrustedRunner 的 1-4 MVP 是否采用本机源码编译 + Swift/compiler/toolchain 证据 + helper digest enrollment 的安装方式，暂不引入签名分发、渠道升级和自动更新系统？ → 同意 (先验证真实 OPN dogfood 闭环；安装摘要必须记录 toolchain、产物 digest 并经 Human 确认，正式发布分发、Apple 签名和自动更新后置。)
- Q: 问题190：Install/verify 是否必须执行真实 helper 自检，验证协议握手、macOS Keychain 不可导出 P-256 密钥签名、公钥指纹、环境/process proof 和临时 key 清理；自检失败保持 install-blocked，不得进入 enrollment？ → 同意 (编译成功不等于 TrustedRunner 可用；只有真实 helper 自检闭合后才允许 Human 看到 enrollment 候选，任何 Keychain、签名、proof 或清理失败都停在安装阶段。)
- Q: 问题191：TrustedRunner 安装验证是否应区分临时自检身份与待 enrollment 的持久身份；临时 key 自检后删除，持久 key 由 Human 明确 provision 并生成 install artifact，但 enrollment 前不能执行，禁止把临时 key 升级为网络信任身份？ → 同意 (身份生命周期分离：self-test 证明本机能力，provision 产生待授权身份，enrollment 才授予 network execution 资格。)
- Q: 问题192：Enroll 是否只能消费 Install/verify + provision 生成的内容寻址 install artifact，不接受手工传入 runner 公钥、helper digest、版本、平台或 capabilities；artifact 缺失、篡改或字段漂移直接 blocked？ → 同意 (安装 artifact 成为 enrollment 的唯一输入边界，绑定 helper、toolchain、持久公钥、Keychain key tag、能力上限和验证证据；Human 只批准 network 授权，不手工重建底层身份。)
- Q: 问题193：TrustedRunner 的可证明能力上限是否应由 helper 版本对应的、代码内固定且版本化的 capability profile 产生，而不是由 CLI 参数或 Human 自行声明；Install artifact 记录 profile digest，Human enrollment 只能选择其子集？ → 同意 (能力 profile 是 helper 实现事实，不是用户输入；profile digest 与 helper digest、版本和 proof 绑定，避免通过声明扩大 TrustedRunner 权限。)
- Q: 问题194：Install artifact、TrustedRunner registry 和 admission binding 的外层 schema 是否从第一天保持 platform-neutral，仅将 macOS 专属信息放入 platform_evidence；Windows CNG、Linux PKCS#11/TPM2 后续复用同一 enrollment/admission contract？ → 同意 (协议外层跨平台稳定，平台差异仅存在于 install/verify、native helper 和 trusted proof；避免把 macOS 实现细节固化进 OPN 核心模型。)
- Q: 问题195：1-4 的首个真实 dogfood 是否选择 ZAgenticLoop 仓库中的有价值、可复核、明确只读任务；真实 Agent 使用 network-allowed + macOS TrustedRunner 完成分析、证据、verification 和 Human review，禁止修改文件、提交或 push；写入型任务后置？ → 同意 (先用只读任务验证真实 OPN execution 闭环和责任边界；写入型 Agent execution 需要后续单独的 capability、资源隔离和验收设计。)
- Q: 问题196：首个只读 dogfood 是否必须采用固定、可验证的任务契约，而不是开放式 prompt；任务为审查 ZAgenticLoop 当前 OPN/TrustedRunner 实现边界，输出包含已实现、未实现、风险、证据引用和建议的结构化 review report，并以字段完整、引用可回读、仓库无修改、无 commit/push、输出未超限作为验收条件？ → 同意 (首个真实任务以结构化 report 和可回读证据为结果，不接受 Agent 自报完成；这为后续 Graph Atom 和写入型任务提供 baseline。)
- Q: 问题197：首个 dogfood review report 是否采用机器可验证 JSON 为事实源、Human 可读 Markdown/文本为渲染视图；JSON 绑定 task/execution/attempt、输入范围、findings、severity、evidence_refs、边界、建议、digest、时间和 provider metadata，Verifier 只消费 schema/digest 校验后的 JSON？ → 同意 (报告事实与展示分离；JSON 进入 EvidenceStore 并参与 verification/review package，Markdown 只是稳定的人类阅读投影，不能反向改变事实。)
- Q: 问题198：首个只读 dogfood 是否必须经过真实 Human review package 和 P-256 签名 Accept 才视为 OPN 任务成功；Verifier 通过只能进入 review-pending，不能自动进入 accepted；package 必须包含 report、证据、verifier 结果、worktree clean、风险和 provider/execution/admission provenance？ → 同意 (OPN 成功终态仍由 Human 对结果负责；自动 verification 只能证明事实完整，不能替代 Human 对结果、风险和边界的最终判断。)
- Q: 问题199：首个只读 dogfood 是否必须绑定不可变仓库输入快照（repository canonical path、branch/ref、base commit、execution worktree、初始 clean digest、roadmap/documentation revision）；执行期间输入漂移直接 blocked 或 outcome-uncertain，禁止把报告归因到不明确代码版本？ → 同意 (报告必须可复现、可归因；Human review 看到的 report、evidence 和 review package 都绑定同一个输入 commit 与 worktree 事实。)
- Q: 问题200：network-allowed 主路径下，是否将只读严格定义为目标仓库与本地 worktree 不可写、无 Git/Provider credential、禁止 commit/push 和本地副作用，但不宣称当前具备 endpoint 级网络副作用阻断能力；无法解释的远程副作用证据进入 outcome-uncertain，不自动接受？ → 同意 (本 milestone 做真实可用与可解释边界：本地只读和凭据清理必须可证明，网络允许的远程副作用治理明确留到后续，不用过度承诺当前能力。)
- Q: 问题201：真实 Agent 的远程模型认证是否采用显式 ProviderAuthRef；worker 不读取任意环境变量、用户目录登录态或 Keychain secret，Provider adapter 只消费受控 runtime 提供的可审计 auth ref（provider、来源类型、权限范围和 digest，不含 secret），缺少 auth ref 直接 blocked？ → 同意 (把“能否调用远程模型”从隐式 credential inheritance 提升为显式 Provider runtime contract；credential broker 和 endpoint 级治理可后置，但不得以环境泄漏或用户目录旁路实现。)
- Q: 问题202：ProviderAuthRef 是否必须绑定单次 network + execution_id + attempt，并具备 provider、audience/scope、issued_at、expires_at 和 revocation 状态；Human approval summary 展示来源类型、scope 和有效期但不展示 secret，任一绑定漂移、过期或撤销都 blocked，禁止自动刷新、换 credential 或重试？ → 同意 (认证引用成为一次 execution 的不可变输入；auth 生命周期独立于 Provider execution，但必须与其绑定，避免远程模型调用绕过 Human approval 和 attempt 边界。)
- Q: 问题203：ProviderAuthRef 是否必须由独立且受 Human 授权的 Provider Auth Broker/Runtime 发行，禁止 Agent、worker 或 Provider 自己声明或自签；MVP 可先实现本机 broker adapter，但必须记录 issuer、发行证据、scope 和 digest，没有可验证 issuer 时直接 blocked？ → 同意 (认证发行权与执行权分离；Provider 只能消费已发行 ref，不能借执行过程给自己授予远程模型访问权限。)
- Q: 问题204：ProviderAuthRef 是否必须是无 secret 的 capability reference，secret 只能留在受控 Provider Auth Runtime 内存或受保护 IPC 通道中，禁止写入 StateStore、EvidenceStore、approval summary/envelope、worker context、stdout/stderr、report 或 review package；泄漏、清理不可证明或 broker IPC 失控进入 blocked 或 outcome-uncertain？ → 同意 (认证事实可审计，认证秘密不可持久化；OPN 记录 issuer/scope/digest/生命周期，不记录或回放 secret。)
- Q: 问题205：Provider Auth Runtime 是否应作为 Human/设备的私有资源，而不是 OPN 网络级共享资源；secret、broker session 和 Provider 登录态只留在持有设备，网络 StateStore 只保存不可取回 secret 的 ProviderAuthRef 元数据，中心责任单元不能读取、复制、迁移或改变 secret 存储位置，跨设备必须重新经过目标设备 Human 授权？ → 同意 (认证秘密的所有权与任务协调权分离；网络可以协调“谁拥有可用认证能力”，不能因为中心节点或任务消费者身份取得认证秘密。)
- Q: 问题206：当被编排节点没有满足任务所需的 ProviderAuthRef 时，路由是否必须停止并返回 Human，而不是自动把任务迁移到其他节点或借用其他设备 credential；Human 决定本地配置授权、改路由、任务降级、修订或终止？ → 同意 (认证能力是节点私有资源，缺失时属于真实路由阻塞；中心责任单元只能解释和提出候选，不能静默迁移权限或秘密。)
- Q: 问题207：1-4 是否应实现 provider-neutral ProviderAuthRuntime 接口，由私有 runtime 提供 inspect、issueRef、launch(ref, execution)、revoke 和 verify；Core、worker、EvidenceStore 不直接处理 credential，首个实现可以是本机 runtime adapter？ → 同意 (Provider 认证成为独立私有生命周期；Core 只编排 opaque ref 和事实，ProviderAuthRuntime 承担 secret、进程启动和清理边界。)
- Q: 问题208：ProviderAuthRuntime 是否应与 TrustedRunner 保持两个独立的本机责任组件；AuthRuntime 负责 credential issue/scope/私有 IPC/revoke/cleanup，TrustedRunner 负责 helper 身份、sandbox、process boundary、worktree、网络和 output proof；execution 必须同时持有两类独立事实，任一缺失或漂移直接 blocked？ → 同意 (认证生命周期与执行环境生命周期解耦但在 execution admission 汇合；AuthRuntime 不能代替 TrustedRunner，TrustedRunner 也不能取得 credential 管理权。)
- Q: 问题209：ProviderAuthRef 的非敏感 metadata 是否必须纳入 AdmissionBoundExecution、LocalExecutionPreflight、approval summary/envelope 和 worker context 的 digest binding（auth_ref_id、provider、issuer、scope/audience、issued_at/expires_at、ref digest、network/execution/attempt）；resume/worker 启动前通过私有 AuthRuntime 重新验证，过期、撤销或状态漂移直接 blocked，secret 不进入任何 artifact？ → 同意 (Human 批准的是明确的 Provider 认证能力而非隐式登录态；auth metadata 与 TrustedRunner admission 一起成为不可变 execution 输入，但秘密仍留在私有 runtime。)
- Q: 问题210：ProviderAuthRef 是否必须绑定具体 OPN 节点与 Provider runtime identity，而不能只绑定 provider；至少包含 network_id、node_id、provider_runtime_id、provider_id、auth_ref_id、scope/audience 和 digest；同一设备上的 Agent1、Agent2 也不能因共用设备自动共享 auth ref？ → 同意 (认证消费主体必须可定位到具体节点/runtime；设备共用不等于权限共用，跨 Agent 使用需要独立 Human 授权和新的 ref。)
- Q: 问题211：1-4 是否冻结为单设备、单 OPN node、单 Human responsibility unit、单 Provider runtime、单 TrustedRunner 的真实只读 dogfood；多设备、多 Agent、跨节点 ProviderAuthRef 转移和 Graph 路由留到下一里程碑？ → 同意 (本阶段验证单节点 OPN Atom 的真实可用性和责任边界，不提前引入多节点协同复杂度；Graph Atom 以后以该 baseline 为基础扩展。)
- Q: 问题212：TrustedRunner 的 install、verify、provision 和 enrollment 是否应通过统一产品级 CLI/API 工作流暴露，不要求 Human 手工执行 swiftc、读取 Keychain 输出或拼 registry mutation；公开 trusted-runner install/verify/provision/enroll/status 等少量动作，并返回结构化状态、Human 摘要、证据引用和下一步？ → 同意 (底层平台细节收敛在产品能力内；Human 负责确认和授权，不承担手工拼接密码学、helper 和 StateStore 事实的操作负担。)
- Q: 问题213：trusted-runner provision 与 enroll 是否都必须要求明确 Human confirmation；install/verify/status 保持可重复、尽量只读；provision 创建持久 Keychain identity，enroll 写入网络级 registry，缺少固定 confirmation 或 Human 签名时禁止任何持久化副作用？ → 同意 (平台密钥创建和网络授权是两种不同副作用，都必须让 Human 看见、确认并留下证据；只读检查不应意外改变身份或网络状态。)
- Q: 问题214：TrustedRunner install/provision/enroll 是否必须具备可恢复、幂等和 fail-closed 语义；同一 helper digest + key tag 不创建第二个 identity，provision 中断先回读验证而不自动生成新 key，同一 artifact + network + runner_id 的重复 enroll 返回已有 mutation，字段或 revision 漂移返回 conflict/blocked，不自动轮换、迁移或删除旧 identity？ → 同意 (身份安装和网络授权也遵循 OPN append-only/CAS 原则；恢复只能重读和验证既有事实，不能在不经 Human 决策时创建新责任边界。)
- Q: 问题215：是否现在冻结 1-4 的设计决策并进入连续实现，依次落地 macOS TrustedRunner install/verify/provision 与 install artifact、Human-signed enroll 和 registry helper metadata、provider-neutral ProviderAuthRuntime/ProviderAuthRef、AuthRef admission/approval/worker binding、真实 network-allowed 只读 dogfood、network-denied 验证、verification/review/Accept/closeout？ → 同意 (1-4 设计冻结，进入实现阶段；按垂直切片闭合真实单节点 OPN Atom，不再继续扩展设计范围，遇到真正边界阻塞再停下 grill。)
- Q: 问题216：ProviderAuthRuntime 与 detached worker 之间的 MVP 私有通道，是否采用本机 Unix domain socket（macOS/Linux）/ named pipe（Windows）的 provider-neutral IPC 抽象；worker 只发送 AuthRef 元数据和 execution binding，Runtime 返回一次性验证结果或受控启动句柄，绝不返回 secret，也不把 secret 注入 worker context、环境变量或 stdout/stderr？ → 同意 (采用 provider-neutral 本机 IPC；macOS/Linux 使用 Unix domain socket，Windows 使用 named pipe。worker 只发送 AuthRef metadata 与 execution binding，Runtime 只返回一次性验证结果或受控启动句柄，secret 不离开私有 runtime。)
- Q: 问题217：IPC 是否必须采用一次性 challenge-response：worker 提交 auth_ref + execution/attempt binding + nonce，Runtime 校验本机 peer identity、AuthRef 状态和 binding 后签发不可复用的 ProviderLaunchHandle；handle 仅绑定一次启动、短时有效、不可迁移，worker 重启或重复消费直接 blocked？ → 同意 (采用一次性 challenge-response 与不可复用 ProviderLaunchHandle；handle 短时有效、绑定本机 peer、network/node/provider/runtime/execution/attempt 和 nonce，重复消费或迁移直接 blocked。)
- Q: 问题218：Provider 启动是否应由 ProviderAuthRuntime 承担，而不是由 worker 直接 spawn Provider？Runtime 使用已验证的 ProviderLaunchHandle 在私有边界内启动 Provider，并通过受控 IPC/stdio relay 提供结果；TrustedRunner 仍负责 worker 的进程边界、worktree、网络策略和输出上限，worker 只能消费受控 Provider channel，不能获得 secret 或原始 credential？ → 同意 (ProviderAuthRuntime 承担 Provider launch 与 credential 注入；TrustedRunner 继续治理 worker 的进程边界、worktree、网络和 output bounds。worker 只能消费受控 Provider channel，不能接触 secret 或原始 credential。)
- Q: 问题219：Provider 结束后，是否要求 ProviderAuthRuntime 先执行并证明 credential cleanup/revoke，再允许 execution 进入 verification-pending；cleanup proof 必须绑定 network/node/provider/runtime/execution/attempt/launch_handle、Provider 退出状态和时间窗口，无法证明清理或 Runtime IPC 中断则进入 outcome-uncertain，禁止自动重试或换 credential？ → 同意 (ProviderAuthRuntime 必须先完成并证明 credential cleanup/revoke；cleanup proof 与 launch handle、Provider 退出状态及 execution binding 关联。清理不可证明或 IPC 中断进入 outcome-uncertain，不自动重试或换 credential。)
- Q: 问题220：ProviderAuthRuntime cleanup proof 与 TrustedRunner post-run proof 是否保持为两份独立、不可互相替代的事实：前者证明 credential/session 已撤销和清理，后者证明 worker/provider 进程树、worktree、网络策略和输出边界；最终 verifier 只有在两份 proof 都通过且绑定同一 execution/attempt 后，才能进入 verification-pending？ → 同意 (两类 proof 独立存在、独立签发、不可互相替代；Verifier 只有在两者都通过且绑定同一 execution/attempt 后才允许 verification-pending。)
- Q: 问题221：ProviderAuthRuntime 与 worker 的受控 channel，是否采用有界、可序列化、provider-neutral 的 framed protocol（如 launch-accepted、stdout、stderr、result、error、exit、cleanup），每帧绑定 execution/attempt/launch_handle 并受大小、顺序和超时约束；Provider 输出只作为事实输入，不能携带或声明 TrustedRunner/AuthRuntime proof？ → 同意 (采用有界 framed protocol；每帧绑定 execution/attempt/launch_handle，校验顺序、大小和超时。Provider 输出只是事实输入，不能自声明 TrustedRunner 或 AuthRuntime proof。)
- Q: 问题222：Provider channel 出现断连、帧乱序、超时或缺少终止/cleanup 帧时，是否立即停止正常完成路径并进入 outcome-uncertain；只允许执行一次有界、只读的 Runtime 状态回读与 cleanup reconciliation，不允许自动重启 Provider、刷新 AuthRef、换 credential 或重放任务？ → 同意 (channel 异常进入 outcome-uncertain；只允许一次有界、只读的 Runtime 状态回读与 cleanup reconciliation，禁止自动重启、刷新 AuthRef、换 credential 或重放任务。)
- Q: 问题223：Human approval 是否必须发生在 AuthRef 已由 Runtime 发行、且 summary 已展示完整非敏感认证事实之后；至少展示 provider/provider_runtime/issuer/audience/scope/issued_at/expires_at/auth_ref_digest/node_id、TrustedRunner identity、network policy 和 execution/attempt，禁止先批准一个之后再替换 AuthRef 的抽象“可用认证能力”？ → 同意 (Human 只批准已发行的具体 AuthRef；summary 展示 provider/runtime/issuer/audience/scope/有效期/digest/node/TrustedRunner/network policy/execution/attempt。AuthRef 不得在批准后被替换。)
- Q: 问题224：AuthRef 在 Human approval 后但尚未启动时过期，是否直接 blocked 并要求新的 execution attempt；若执行过程中到期，是否停止继续发起新的 Provider 请求，触发 Runtime cleanup，并根据是否取得完整 cleanup proof 进入 outcome-uncertain 或后续验证，禁止自动延长有效期？ → 同意 (AuthRef 不自动延长；启动前过期直接 blocked。执行中到期停止新的 Provider 请求并触发 cleanup，按 cleanup proof 完整性进入后续验证或 outcome-uncertain；需要继续时创建新 attempt。)
- Q: 问题225：本机 IPC endpoint 是否必须避免固定公共路径和 TCP 监听：Runtime 每次 execution 创建随机、短生命周期 endpoint，置于仅 Human/Runtime 可访问的私有目录（Unix socket 0600；Windows named pipe ACL），并结合 OS peer identity 与 challenge nonce 校验；路径、socket 文件或 pipe 名称本身不能作为唯一信任依据？ → 同意 (采用随机、短生命周期 IPC endpoint；Unix socket 使用私有目录和 0600，Windows named pipe 使用 ACL，并结合 OS peer identity 与 challenge nonce。endpoint 名称本身不构成信任。)
- Q: 问题226：ProviderAuthRuntime 自身的 issuer/runtime identity，是否采用独立于 TrustedRunner 的本机 provision 流程：Runtime 生成不可导出的 P-256 identity key，记录 runtime binary/version/provider capability digest，由 Human 显式确认并保存本机 manifest；IPC challenge-response 由该 identity 签名，worker 校验 runtime identity、manifest digest 和 provider capability，不能仅凭“本机进程自称 Runtime”作为可信 issuer？ → 同意 (ProviderAuthRuntime 采用独立本机 provision；生成不可导出的 P-256 identity，Human 确认 runtime manifest。IPC challenge-response 与 AuthRef issuer 都绑定 runtime identity、manifest digest 和 provider capability，不能依赖进程自报。)
- Q: 问题227：AuthRef 发行时，Runtime 是否必须同时校验三层能力边界：Human 明确批准的 scope、Runtime manifest 声明的 provider capability 上限、当前 execution 所请求的 scope；三者不一致或请求超集直接拒绝，不静默降级 scope、不自动扩大 scope，也不复用其他 AuthRef？ → 同意 (AuthRef scope 必须同时满足 Human 批准、Runtime manifest capability 上限和 execution 请求；超集或不一致直接拒绝，不静默降级、扩大或复用其他 AuthRef。)
- Q: 问题228：Human 撤销权限时，是否要求 Runtime 支持分层撤销并 fail-closed：撤销单个 AuthRef 只影响该 execution，撤销 Runtime identity/manifest 影响其全部 AuthRef，撤销 Human 授权源影响该授权源发行的全部 AuthRef；worker/Provider 下一次 IPC 交互必须立即收到 revoked，禁止继续使用已有 launch handle？ → 同意 (支持 AuthRef、Runtime identity/manifest、Human 授权源三层撤销；撤销立即阻断后续 IPC 与已有 launch handle，不允许继续消费。)
- Q: 问题229：若 AuthRef/Runtime/Human 授权在 Provider 已运行期间被撤销，Runtime 是否必须向 Provider 发送受控终止并阻断后续 channel；终止结果和 credential cleanup 必须生成 proof，若 Provider 或 IPC 无法确认已终止，则进入 outcome-uncertain，禁止把撤销误判为正常完成？ → 同意 (运行中撤销触发 Provider 受控终止并阻断 channel；终止与 credential cleanup 都必须有 proof，无法确认终止进入 outcome-uncertain，不得视为正常完成。)
- Q: 问题230：Framed IPC 的 MVP 编码是否采用 4-byte big-endian length prefix + canonical JSON UTF-8 payload，而不是裸 JSONL；每帧限制最大字节数、schema/version、sequence、request/response correlation，并拒绝长度溢出、重复、乱序、未知字段和非 UTF-8 数据？ → 同意 (采用 4-byte big-endian length prefix + canonical JSON UTF-8 payload；帧带 schema/version、sequence、correlation，受最大长度约束，拒绝溢出、重复、乱序、未知字段和非 UTF-8。)
- Q: 问题231：由于 Node.js 本身不能跨平台直接可靠取得 Unix socket / named pipe 的 OS peer identity，MVP 是否采用 OS 原生 peer identity adapter + Runtime 签发 bootstrap challenge 双重校验：macOS 由 native helper 提供 process audit identity，Linux 使用 peer credentials，Windows 使用 named-pipe impersonation/token identity；无法取得平台级 peer identity 时直接 blocked，不降级为仅凭随机 token？ → 同意 (采用 OS 原生 peer identity adapter 与 Runtime bootstrap challenge 双重校验；平台级 peer identity 不可取得时直接 blocked，不降级为仅凭随机 token。)
- Q: 问题232：Runtime bootstrap challenge 是否必须是一次性、短时、仅内存保存的启动事实，绑定 runtime_identity + worker_process_identity + network/node/provider/execution/attempt + endpoint + nonce；不得写入 StateStore/EvidenceStore、环境变量或命令行，超时、断连、重复消费和 worker PID/identity 漂移直接失效？ → 同意 (bootstrap challenge 仅在 Runtime 内存中短时存在且一次性消费，绑定 runtime/worker/process/execution 全部身份与 endpoint；超时、断连、重复消费或 identity 漂移直接失效，不进入任何 artifact。)
- Q: 问题233：每次 Provider execution 结束或异常时，Runtime 是否必须清理 endpoint、bootstrap challenge、launch handle 和临时 IPC state，并生成仅含 metadata/digest 的 cleanup fact；清理后 endpoint 不可重新连接，任何残留或无法证明清理都进入 outcome-uncertain？ → 同意 (Runtime 必须清理 endpoint、bootstrap challenge、launch handle 与临时 IPC state，并生成 metadata/digest cleanup fact；清理后 endpoint 不可复用，残留或清理不可证明进入 outcome-uncertain。)
- Q: 问题234：cleanup fact 是否必须由 ProviderAuthRuntime identity 签名，并绑定 runtime_manifest_digest、AuthRef digest、launch_handle digest、endpoint digest、execution/attempt、Provider exit fact、cleanup timestamps 和清理结果；Verifier 不接受 worker 或 Provider 自报的 cleanup fact？ → 同意 (cleanup fact 必须由 Runtime identity 签名并绑定完整 execution/attempt 与 endpoint/handle/manifest facts；Verifier 不接受 worker 或 Provider 自报。)
- Q: 问题235：Runtime identity 被撤销或 manifest digest 发生漂移后，历史 cleanup fact 是否仍可作为过去发生过的事实进行签名校验，但不得再用于新的 execution admission、launch 或 AuthRef issuance；新的执行必须重新 provision/verify，并获得新的 Human authorization？ → 同意 (历史 cleanup fact 保持可审计但不可授权复用；Runtime identity/manifest 撤销或漂移后，新的 execution 必须重新 provision/verify 并取得新的 Human authorization。)
- Q: 问题236：ProviderAuthRuntime 升级是否采用新 manifest、新 runtime identity、新 provision/verify 模型，而不是原地更新 manifest；旧 Runtime 可继续负责已启动 execution 的终止与 cleanup，但不得发行新 AuthRef，升级完成后必须由 Human 明确确认新的 Runtime identity 才能恢复执行？ → 同意 (Runtime 升级创建新的 manifest/identity 并重新 provision/verify；旧 Runtime 仅负责既有 execution 的终止与 cleanup，不再发行新 AuthRef。新 Runtime 必须重新 Human 确认后才能执行。)
- Q: 问题237：ProviderAuthRuntime launch 是否应接收不可变 execution contract（provider、execution/attempt、worktree、goal digest、network policy、output limits 和 adapter digest），由 Runtime 自己选择固定 Provider invocation；worker 只能通过 framed channel 发送已绑定的结构化 task payload，不能传入任意 executable、argv、环境变量或 credential 参数？ → 同意 (Runtime launch 只接受不可变 execution contract，并固定 Provider invocation；worker 只能发送绑定 execution 的结构化 task payload，不得注入 executable、argv、环境变量或 credential。)
- Q: 问题238：为保持进程证明闭合，是否由 TrustedRunner 先启动 worker 与本次 execution 专属的 ProviderAuthRuntime sidecar，并将二者放入同一个受控 process boundary；Runtime 再在该 boundary 内启动 Provider。这样 TrustedRunner 负责观察 worker、Runtime、Provider 全部后代，Runtime 负责 credential/launch/cleanup，二者各自产生独立 proof？ → 同意 (TrustedRunner 启动本次 execution 专属的 Runtime sidecar 与 worker，并将 Runtime/worker/Provider 纳入同一 process boundary；Runtime 与 TrustedRunner 各自产生独立 proof，职责不合并。)
- Q: 问题239：Runtime sidecar 是否必须在 TrustedRunner launch 前完成独立 runtime manifest verification，并将 runtime_identity_fingerprint + runtime_manifest_digest + provider capability digest 纳入 execution admission；sidecar 只能消费与该 admission 完全匹配的 contract，未 provision、manifest 漂移或 capability 超集直接 blocked？ → 同意 (Runtime sidecar 在 TrustedRunner launch 前完成独立 manifest verification；runtime identity、manifest digest、provider capability 纳入 admission，任一缺失、漂移或超集直接 blocked。)
- Q: 问题240：sidecar/worker/Provider 是否采用严格启动顺序：TrustedRunner 验证并启动 Runtime sidecar → sidecar 完成 peer identity handshake 和 execution contract verification → sidecar 返回 runtime-ready → TrustedRunner 启动 worker → worker 完成 AuthRef challenge → Runtime 签发一次性 launch handle → Runtime 启动 Provider；任一步失败都不启动后续进程？ → 同意 (采用严格启动顺序；Runtime 未 ready、contract/peer/AuthRef challenge 任一步失败，都不启动后续进程并进入对应 blocked 状态。)
- Q: 问题241：sidecar 启动链中，若 Runtime 在启动前或 handshake 阶段失败，是否统一进入 blocked；若 Runtime 已启动 Provider 后才失联、无法确认终止或 cleanup，则进入 outcome-uncertain；两者都必须记录失败阶段、已启动进程、endpoint、challenge/launch handle 是否消费及 cleanup 结果，禁止把启动前失败与运行中不确定性混为同一状态？ → 同意 (启动前或 handshake 失败进入 blocked；Provider 已启动后若无法确认终止/cleanup 进入 outcome-uncertain。两类状态分别记录阶段、进程、endpoint、handle/challenge 和 cleanup facts。)
- Q: 问题242：正常退出是否采用固定逆序：Provider 发送终态 → Runtime 完成 credential revoke/cleanup → Runtime 关闭 Provider channel 并确认 Provider 退出 → worker 写入 bounded result 并退出 → Runtime 清理 endpoint/challenge/handle 并退出 → TrustedRunner 收集最终 process-boundary observation；任一环节顺序或确认缺失都不进入 verification-pending？ → 同意 (正常退出采用固定逆序；Provider/credential/channel/worker/Runtime/TrustedRunner 各阶段必须确认，任一缺失不得进入 verification-pending。)
- Q: 问题243：异常退出或撤销时，是否允许 TrustedRunner 立即终止整个 process boundary（Provider、Runtime、worker），但必须将先清理后终止与强杀后无法确认 cleanup 区分记录；强杀只是停止副作用，不等于 credential cleanup 成功，最终仍需 Runtime reconciliation，无法证明时进入 outcome-uncertain？ → 同意 (强杀只停止 process boundary，不证明 credential cleanup；区分 cleanup-before-terminate 与 forced-terminate-uncertain，最终需 Runtime reconciliation，无法证明进入 outcome-uncertain。)
- Q: 问题244：强杀后 Runtime reconciliation 是否必须是单次、有界、只读的本机状态查询，由原 Runtime identity 或已登记的 recovery verifier 签名；不得通过重新启动 Provider、重新读取 credential、自动刷新 AuthRef 或恢复原 execution 来补齐 cleanup proof？ → 同意 (强杀后只允许一次有界、只读 reconciliation；由原 Runtime identity 或登记的 recovery verifier 签名，禁止重启 Provider、读取 credential、刷新 AuthRef 或恢复 execution。)
- Q: 问题245：原 Runtime 已失联或 identity 已撤销时，是否允许已预先登记且能力受限的 recovery verifier 只证明 endpoint/process/credential 状态可观察结果，但不能发行 AuthRef、启动 Provider、恢复 execution 或替代 Human Accept；若 recovery verifier 也无法证明完整 cleanup，最终保持 outcome-uncertain？ → 同意 (允许预先登记的受限 recovery verifier 只提供终态观察 proof；不得发行、启动、恢复或替代 Human Accept。无法证明完整 cleanup 时保持 outcome-uncertain。)
- Q: 问题246：Recovery verifier 是否必须由 Human 显式 provision/enroll，并绑定固定的 verifier identity、平台能力 profile、观察范围和版本 digest；其权限只能是 read-only reconciliation，不能通过配置或运行时参数扩大为 Runtime/TrustedRunner/Provider 权限？ → 同意 (Recovery verifier 由 Human 显式 provision/enroll，绑定 identity、profile、观察范围和版本 digest；权限固定为只读 reconciliation，不可配置扩权。)
- Q: 问题247：Recovery verifier 即使证明 cleanup 完整，是否也只能把 execution 从 outcome-uncertain 推进到 review-pending 或 human-reconciliation-ready，必须由 Human 重新 review/Accept 才能形成成功终态；不得自动恢复为 provider-completed 或跳过原有 review package？ → 同意 (Recovery proof 只能产生 human-reconciliation-ready/review-pending 输入；必须重新 Human review/Accept，不得自动恢复 provider-completed 或跳过 review package。)
- Q: 问题248：Recovery proof 是否必须作为新的 append-only evidence 纳入 reconciliation package，并重新计算 review package digest；原 review package 保持不可变，Human Accept 必须明确绑定原执行事实 + recovery proof + 新 package digest，禁止原地修改旧 package 或复用旧 Accept？ → 同意 (Recovery proof 作为 append-only evidence 进入新的 reconciliation/review package；旧 package/Accept 不变，新 Accept 必须绑定原执行事实、recovery proof 与新 digest。)
- Q: 问题249：为避免 1-4 无限扩展，当前里程碑是否只实现 ProviderAuthRuntime IPC 的主路径与 fail-closed 状态：verify → challenge → launch → framed channel → revoke/cleanup → 独立 proof；Recovery verifier、强杀后 reconciliation、Runtime identity upgrade migration 只记录稳定协议和状态契约，作为下一子节点实现，不在当前 slice 继续扩张？ → 同意 (当前 slice 只闭合 Runtime IPC 主路径与 fail-closed cleanup proof；Recovery verifier、强杀后 reconciliation、Runtime identity upgrade migration 后置实现，仅保留协议/状态契约。)
- Q: 问题250：当前 1-4 的 IPC slice 设计是否已经足够冻结，现在开始实现 ProviderAuthRuntime verify → challenge → launch → framed channel → revoke/cleanup → proof 主路径，并把 recovery verifier 与 Runtime upgrade migration 留到后续子节点？ → 同意 (开始实现当前 IPC 主路径；recovery verifier 与 Runtime upgrade migration 后置。)
- Q: 问题251：ProviderAuthRuntime sidecar 是否应采用 provider-neutral ProviderRuntimeAdapter 接口：sidecar 只负责 AuthRef 校验、launch handle、私有 credential 注入、channel relay 和 cleanup；具体 Codex/Workbuddy 等 Provider invocation 由 adapter 实现，adapter 不得接触 Human approval、TrustedRunner registry 或直接写 StateStore？ → 同意 (sidecar 与 ProviderRuntimeAdapter 解耦；sidecar 管 AuthRef/channel/cleanup，adapter 只管固定 Provider invocation，不接触 Human approval、TrustedRunner registry 或 StateStore。)
- Q: 问题252：ProviderRuntimeAdapter 获取 credential 是否采用一次性受保护 IPC handle（Unix fd / Windows handle）注入，而不是命令行、环境变量、临时文件或普通 stdin；adapter 只能在 Runtime sidecar 的私有 process boundary 内消费该 handle，退出后 handle 立即关闭并进入 cleanup proof？ → 同意 (adapter 通过一次性受保护 IPC handle 获取 credential；禁止 argv/env/文件/普通 stdin。handle 只在 sidecar 私有边界内消费，退出后关闭并纳入 cleanup proof。)
- Q: 问题253：远程模型 Provider 的 adapter 是否必须继承同一 execution 的 coarse network policy，并与 worker/Runtime 处于同一 TrustedRunner 网络边界；禁止 adapter 自建代理、隧道、备用 DNS、第二网络 namespace 或绕过 TrustedRunner 的网络策略？当前不做 endpoint 级治理，但必须阻止另开网络通道绕过 execution policy。 → 同意 (adapter 继承同一 TrustedRunner coarse network policy，不得另开代理、隧道、备用 DNS、网络 namespace 或其他旁路；endpoint 级治理后置。)
- Q: 问题254：在 network-allowed 下，adapter 的远程请求是否只记录可验证的 coarse network facts（policy digest、TrustedRunner network proof、Provider channel/exit 状态、无法解释的副作用标记），不伪造 endpoint 级审计；若发生未能解释的远程副作用或网络终态不明，execution 进入 outcome-uncertain，不能自动 Accept？ → 同意 (network-allowed 只记录 coarse network facts，不伪造 endpoint 审计；远程副作用或网络终态不可解释时进入 outcome-uncertain，不自动 Accept。)
- Q: 问题255：ProviderRuntimeAdapter 的结果是否必须通过 provider-neutral ProviderResult schema 返回，只允许 bounded result、stdout/stderr digest、exit status、usage metadata 和 evidence refs；禁止返回 credential、AuthRef secret、Runtime handle、未验证的环境声明或未经 Runtime/TrustedRunner 签名的 proof？ → 同意 (ProviderResult 只允许 bounded result、输出 digest、exit/usage/evidence facts；禁止 credential、secret、handle、环境自报或未签名 proof。)
- Q: 问题256：ProviderRuntimeAdapter 崩溃、超时、返回 malformed result 或违反 frame contract 时，是否立即停止正常完成路径并记录 adapter failure fact；若 Provider 已启动则先进入 Runtime cleanup，cleanup 不可证明时为 outcome-uncertain，启动前失败则为 blocked，禁止 adapter 自动重启或换 Provider？ → 同意 (adapter 异常启动前进入 blocked，启动后先 cleanup；cleanup 不可证明进入 outcome-uncertain。记录 failure fact，禁止自动重启或换 Provider。)
- Q: 问题257：ProviderRuntimeAdapter 的固定 invocation 身份是否必须以 adapter_id、adapter_version、binary_digest、argv_policy_digest 和 invocation_digest 组成不可变 adapter contract，并同时纳入 execution contract、Human approval summary/envelope、worker context、launch handle 与 cleanup proof；任一漂移、缺失或无法验证都在 Provider 启动前 blocked，禁止运行时静默替换 adapter？ → 同意 (adapter invocation digest 是执行身份的一部分；ProviderRuntimeAdapter 只能按已批准的固定 contract 运行，不能由 adapter 自报或在启动后变更。)
- Q: 问题258：ProviderRuntimeAdapter 返回值是否必须先经过统一 provider-neutral ProviderResult validator；旧格式、未知字段、超界 result、状态与 success 矛盾或无法归一化时不得进入 verification-pending，Provider 已启动则记录 adapter-failure 并执行 cleanup，cleanup 不可证明为 outcome-uncertain，禁止 fallback 或自动重试？ → 同意 (ProviderResult validator 是 adapter 与生命周期之间的唯一结果边界；失败按已启动/未启动分别进入 cleanup/outcome-uncertain 或 blocked。)
- Q: 问题259：Provider 已启动后的 adapter failure 是否必须由 TrustedRunner/ProviderAuthRuntime 注入受信任 cleanup coordinator；cleanup 返回可验证 proof 才允许将 adapter failure 定位为 blocked，cleanup 缺失、失败或 proof 不可验证必须升级为 outcome-uncertain，cleanup fact 纳入 provider-result evidence，adapter 不得自报 cleanup？ → 同意 (cleanup coordinator 属于编排边界，不属于 Provider adapter；结果状态必须反映 cleanup 可证明性。)
- Q: 问题260：是否应提供一个 provider-neutral Runtime cleanup coordinator factory，将已发行的具体 ProviderLaunchHandle 与 execution/attempt binding 绑定到 ProviderAuthRuntime.cleanup；仅 Runtime 返回有效 cleanup_digest 时返回 cleaned proof，Runtime 拒绝、绑定漂移或异常统一返回 uncertain，不暴露 secret 或允许 adapter 自报 cleanup？ → 同意 (该 factory 只做 Runtime cleanup 事实到 runner coordinator 的受控投影，不改变 Runtime 的权限边界。)
- Q: 问题261：真实 worker CLI 是否必须只通过 execution context 中已发行且绑定 adapter_contract_digest 的 ProviderLaunchHandle，加上本次 execution 专属的 Runtime IPC endpoint/correlation binding，创建 cleanup coordinator；handle/context 漂移或绑定不完整在 Provider 启动前 blocked，Provider 已启动后的 Runtime IPC 不可用、超时或响应漂移进入 outcome-uncertain？ → 同意 (worker 不直接持有 ProviderAuthRuntime，也不注入内存 Runtime；只通过受控 provider-neutral IPC client 请求 Runtime cleanup。启动前拒绝不完整或漂移的 handle/context，运行后 cleanup IPC 无法证明则保持 outcome-uncertain。)
- Q: 问题262：是否现在冻结并实现以下约束：真实 worker CLI 不再直接 spawn Provider；必须由本次 execution 专属的 ProviderAuthRuntime sidecar 完成 verify、challenge、launch、framed result channel 和 cleanup。缺少 sidecar、launch handle 或 channel 在 Provider 启动前 blocked；Provider 已启动后 channel 断连、乱序、超时或 cleanup 不可证明进入 outcome-uncertain。现有本地 Provider adapter 仅保留为 Runtime/协议测试 fixture，不作为真实 worker 的旁路执行入口？ → 同意 (先实现 provider-neutral Runtime IPC launch/channel 垂直切片；Runtime 负责 Provider launch 与 channel，worker 不直接 spawn。TrustedRunner post-run proof 仍保持独立，不接受 Runtime 或 Provider 自报替代。)
- Q: 问题263：Runtime bootstrap challenge 是否必须增加独立的短时内存 TTL（推荐 30 秒），超时未消费直接 blocked，消费后立即失效，且不写入任何持久化 artifact？ → 同意 (采用 30 秒内存 TTL；challenge 只在 Runtime 内存中短时存在，消费后立即失效，超时、断连、重复消费和身份漂移直接 blocked。)
- Q: 问题264：Runtime identity fingerprint、runtime manifest digest 与 provider capabilities digest 是否必须纳入 sidecar admission、bootstrap challenge、launch handle 和 cleanup proof 的不可变绑定？ → 同意 (sidecar 启动前必须校验三类 Runtime identity/capability digest；任一缺失、漂移或 capability 超集直接 blocked，不能继续使用旧 challenge 或 launch handle。)
- Q: 问题265：在当前没有线上用户的前提下，ProviderLaunchHandle 与 ProviderCleanupProof 是否直接 enrich 现有 v1 schema，新增三个必填 Runtime identity/capability digest，而不做 v2 schema upgrade？ → 同意 (采用 enrich；现有 v1 直接增加 runtime_identity_fingerprint、runtime_manifest_digest、provider_capabilities_digest 必填字段，并同步更新所有 producer、validator、client 和 fixture。)
- Q: 问题266：三个 Runtime digest 是否应作为 AdmissionBoundExecution.binding.runtime_binding 的必填字段，由 Runtime provision/admission 阶段生成并冻结，再传递到 approval summary、worker context、challenge、launch handle 和 cleanup proof？ → 同意 (将 runtime_identity_fingerprint、runtime_manifest_digest、provider_capabilities_digest 作为 admission binding 的必填 runtime_binding；在 Provider 启动前冻结并贯穿 approval、worker、challenge、launch 和 cleanup。)
- Q: 问题267：是否进入真实 Runtime sidecar 编排切片：由 real-agent-dogfood resume 创建并启动本次 execution 专属的 ProviderAuthRuntime sidecar，生成真实 IPC endpoint/correlation，写入 worker context，并由 sidecar 调用固定 ProviderRuntimeAdapter；禁止继续用空缺的 provider_runtime_ipc 让 worker 进入 running？ → 同意 (进入真实 Runtime sidecar 编排；resume 必须为本次 execution 建立专属 sidecar、endpoint、challenge/launch/cleanup 生命周期，并让 worker 只通过该 sidecar 执行。)
- Q: 问题268：首个真实 sidecar 是否采用 sidecar 自己持有私有 AuthRuntime，Human 显式批准后通过一次性受保护 stdin/Unix FD 注入 Provider secret；secret 不进入 StateStore、EvidenceStore、worker context 或命令行？ → 同意 (采用一次性受保护 stdin/Unix FD 注入；sidecar 在内存中持有 secret 并发行 AuthRef，所有持久化 context、StateStore、EvidenceStore、命令行和 stdout/stderr 均不包含 secret。)
- Q: 问题269：是否采用继承的 Unix FD 作为 secret 输入，例如 --provider-secret-fd 3，由 Human 在启动 resume 前打开 pipe/FD 3；resume 将该 FD 原样传给 sidecar，sidecar 只读一次后立即关闭，resume、worker context、命令行参数和持久化 artifact 都不接触 secret？ → 同意 (采用继承 FD 的一次性 secret 输入；sidecar 只读一次并立即关闭，secret 不进入 resume、worker、StateStore、EvidenceStore 或命令行参数。)
- Q: 问题270：是否先实现 macOS Unix socket process-audit peer identity adapter，再接入真实 sidecar；Linux peer credentials、Windows named-pipe token adapter 后续复用同一接口？ → 同意 (先实现 macOS 原生 process-audit adapter；平台无关接口保持稳定，无法获取真实 OS peer identity 时 fail-closed，Linux/Windows adapter 后置。)
- Q: 真实 macOS peer identity adapter 首先接入哪条 IPC 边界？ → A：先接入 ProviderAuthRuntime sidecar，验证 TrustedRunner → Runtime sidecar → Provider 的真实 peer handshake，再推进 post-run proof IPC。 (sidecar 位于 Provider 启动前，是严格启动顺序的前置门槛；先闭合该门槛可避免把 post-run 证据问题与 Provider 启动问题耦合。)
- Q: ProviderAuthRuntime sidecar 的 expected_peer_identity_digest 如何获得？ → A：TrustedRunner 启动 worker 后查询其 PID 的真实 macOS identity，再通过一次性受保护 FD 注入 sidecar；sidecar 不采用 TOFU、随机 token 或 worker 自报身份。 (digest 绑定 worker 进程实例；真实 OS identity 由 TrustedRunner/native adapter 采集，sidecar 只接受受保护启动契约注入的预期值并对连接 peer 再验证。)
- Q: sidecar 先启动但 worker identity 尚未知时，如何保持严格启动顺序？ → A：sidecar 先以 awaiting-peer-binding 状态启动，通过一次性受保护 FD 等待 TrustedRunner 注入 worker identity binding；binding 校验通过后才进入 runtime-ready 并接受业务连接。 (保留 TrustedRunner → sidecar → worker 的生命周期顺序，同时避免 TOFU；sidecar 在 binding 到达前不接受 ProviderAuth 业务连接。)
- Q: sidecar 的 identity bootstrap binding 使用哪种受保护通道？ → A：使用独立继承 Unix FD 4，传递一次性 bounded canonical JSON identity binding；FD 3 保留给 Provider/Auth secret。FD 4 读取一次后关闭，缺失、重复、超时或漂移直接 blocked。 (分离 secret 与身份绑定的权限、生命周期和审计边界；binding 绑定 network/node/provider、execution/attempt、worker PID、identity digest、nonce 和 binding digest。)
- Q: FD 4 identity binding 是否需要额外 TrustedRunner 签名？ → A：不额外签名；采用继承 FD authority + canonical binding digest。sidecar 只接受 TrustedRunner 继承的 FD 4 一次性输入，并校验 schema、字段、nonce、binding digest 和启动契约。 (本机一次性 bootstrap 的发送方边界由 FD 所有权提供；额外签名、runner 公钥注册与轮换留给跨设备/跨网络协议，不扩大当前 MVP。)
- Q: sidecar 在 identity binding 到达前是否创建业务 Unix socket？ → A：不创建；start 阻塞等待并校验 FD 4 binding，只有校验通过后才创建业务 socket、进入 runtime-ready 并返回；缺失、超时或非法直接启动失败。 (runtime-ready 表示 identity bootstrap、execution contract 和 peer gate 已闭合，避免未就绪 endpoint 引入连接竞态。)
- Q: bootstrap binding 是否拆成独立 provider-neutral 协议模块？ → A：拆分为 provider-neutral bootstrap binding 协议模块与 OS-specific transport；当前实现 macOS inherited Unix FD，后续 Linux/Windows 复用同一协议接口。ProviderAuth sidecar 只消费已验证 binding。 (隔离 canonical envelope、digest、状态机与平台 FD/named-pipe 传输，避免 sidecar 直接耦合 macOS 细节。)
- Q: bootstrap binding 是否拆成独立 provider-neutral 协议模块？ → A：拆分为 provider-neutral bootstrap binding 协议模块与 OS-specific transport；当前实现 macOS inherited Unix FD，后续 Linux/Windows 复用同一协议接口。ProviderAuth sidecar 只消费已验证 binding。 (隔离 canonical envelope、digest、状态机与平台 FD/named-pipe 传输，避免 sidecar 直接耦合 macOS 细节。)
- Q: worker 已启动但 identity bootstrap 失败时如何收敛生命周期？ → A：TrustedRunner 执行一次有界的 worker process-tree termination；sidecar 不进入 runtime-ready，worker 不得启动 Provider。能证明完整终止则 blocked，无法证明则 outcome-uncertain 并交 Human review；不自动重试，不引入 recovery verifier。 (这是启动前置条件失败的有界清理，不扩展为新的 recovery 机制；必须保留 process-boundary 证据。)
- Q: bootstrap 失败清理是否复用现有 TrustedRunner process-boundary 机制？ → A：复用并扩展现有 TrustedRunner process-boundary 机制；sidecar/worker bootstrap 只新增 lifecycle outcome/proof 投影，不另写 kill、等待和 descendant 检查逻辑。 (TrustedRunner 继续是唯一 process-boundary 责任单元，保证 SIGTERM、grace period、SIGKILL、descendant、orphan/unknown 检查及 outcome 映射一致。)
- Q: sidecar 与 worker 的 process boundary 如何组织？ → A：sidecar、worker 及其 Provider descendants 共用一个 TrustedRunner-owned execution process group；TrustedRunner 统一终止和证明整个 execution group。 (单一 execution boundary 可复用现有 process_boundary 证据，bootstrap 失败时一次清理覆盖 sidecar、worker 和 descendants。)
- Q: 由谁创建 execution process group 并让 sidecar/worker 加入？ → A：扩展现有 macOS TrustedRunner native helper；由 native helper 创建 execution process group，sidecar 作为 group leader，worker 通过受控 posix_spawn 加入，Provider descendants 继承该 group。 (继续强化现有 TrustedRunner native boundary，统一 group identity、bootstrap FD 和 process-boundary proof，避免新增并行 supervisor 实现。)
- Q: sidecar 完成 identity binding 后如何宣告 runtime-ready？ → A：通过独立一次性 inherited FD 5 返回 bounded readiness envelope；FD 4 是 TrustedRunner→sidecar binding，FD 5 是 sidecar→TrustedRunner runtime-ready/blocked。 (sidecar 只有完成 FD 4 校验、socket 创建保护和 execution contract 校验后才能返回 runtime-ready，否则返回结构化 blocked。)
- Q: runtime-ready envelope 需要绑定哪些内容？ → A：绑定完整启动上下文：network/node/provider、execution/attempt、sidecar PID、identity_binding_digest、runtime_binding_digest、correlation ID、socket path/digest、Unix socket 0600 权限证明和 readiness digest；TrustedRunner 逐项校验。 (runtime-ready 是可验证的 execution-bound evidence，而不是进程存活信号；endpoint 位置本身不构成信任。)
- Q: TrustedRunner 如何在 worker 首次连接前获取 expected identity digest？ → A：扩展 macOS process-audit helper 支持 --pid <pid>；TrustedRunner 启动 worker 后先查询 PID identity 并生成 expected digest，再经 FD 4 注入 sidecar；sidecar 对实际 socket peer 重新采集并比对。 (预启动 PID inspection 与连接时 socket peer verification 是两次独立 OS 事实采集；digest 绑定 PID，可检测进程退出、PID reuse 和 peer drift。)
- Q: FD 3/4/5 的编号是否属于跨平台协议？ → A：不属于；provider-neutral 协议只定义 channel role 和 envelope，Unix 默认 FD 3/4/5，Windows 后续映射为对应 named pipe/handle。 (避免 Unix FD 细节泄漏到跨平台协议；实际 FD 编号由 Unix launch contract 显式传入或采用 MVP 默认值。)
- Q: FD 4/5 的一次性 envelope 采用什么 framing？ → A：4-byte big-endian length + 单帧 canonical JSON，最大 16 KiB；读取完成后必须关闭 FD，超长、半帧、追加第二帧、非 canonical 或超时直接 blocked。 (与现有 bounded IPC frame 一致，明确输入上限和单次消费边界，避免启动挂起与多帧注入。)
- Q: bootstrap envelope 的 canonicalization 和 digest 如何定义？ → A：复用现有 JCS/RFC 8785 canonicalization；固定 schema/version/profile，digest 统一 sha256:<64 hex>，不包含自身字段，sender/receiver 对同一 unsigned payload 计算。 (避免 JSON.stringify 或 native 自定义拼接造成跨语言 digest 漂移，直接复用项目现有 canonicalization contract。)
- Q: identity_digest 与 execution_binding_digest 如何分工？ → A：分离两类 digest；identity_digest 只覆盖 PID 与 macOS signing identity，execution_binding_digest 覆盖 network/node/provider、execution/attempt、worker、identity_digest 和 runtime binding。 (OS identity 是事实层，execution binding 是授权/编排层；peer verifier 保持 provider-neutral，sidecar 再校验二者组合关系。)
- Q: Runtime sidecar 自身的身份如何被信任？ → A：sidecar executable identity 由 TrustedRunner registry 与 process boundary 证明；sidecar digest 预注册并由 native helper 启动时校验，sidecar PID/group 纳入 readiness envelope；worker peer identity 仍独立验证。 (TrustedRunner proof 负责 sidecar，OS peer adapter 负责 worker，Runtime/Auth proof 保持独立。)
- Q: TrustedRunner registry 如何建模 Runtime sidecar？ → A：新增独立 runtime-sidecar artifact/identity 类型；sidecar 自有 executable digest、version、adapter contract、capability profile、verification evidence 和 active/revoked 状态，由 TrustedRunner 负责启动与 boundary proof，但不冒充 TrustedRunner。 (分离 TrustedRunner 环境边界责任与 Runtime/Auth 执行组件身份，保留可注册、撤销和能力审计。)
- Q: Runtime sidecar artifact 的注册和撤销是否必须由 Human 批准？ → A：必须 Human-signed registry mutation；注册、能力变更、adapter contract、executable digest 和撤销都需 Human approval，Agent 不能自行注册、升级或恢复。 (sidecar 是受信执行组件，必须沿用 TrustedRunner registry 的 Human authority 和 active/digest gate。)
- Q: sidecar registry 在 execution 运行期间发生撤销或变更时如何处理？ → A：execution admission 时冻结 sidecar artifact digest、version、capability 和 registry revision；当前 execution 使用不可变 snapshot，撤销和漂移只阻止后续 execution；紧急强杀另设 Human-approved control。 (避免运行中 registry 轮询造成隐式竞态，同时保留后续 execution 的撤销保护。)
- Q: sidecar 的 FD 3 secret 校验与 FD 4 worker identity binding 如何排序？ → A：FD 5 作为有界两阶段 status channel，最多返回 sidecar-auth-ready 和 runtime-ready 两帧；TrustedRunner 只在 auth-ready 后启动 worker，只在最终 runtime-ready 后允许 worker 连接；任意 blocked、乱序、第三帧或超时失败。 (修正单帧 readiness 的过窄设计，同时保持 bounded、单向、不可重放的启动状态通道。)
- Q: FD 5 readiness status channel 的写端和继承边界如何控制？ → A：只有 sidecar 持有 FD 5 写端，TrustedRunner 持有读端；worker/Provider 不继承，sidecar 写完两帧立即关闭，并由 CLOEXEC、spawn file actions 和 launch contract 验证继承边界。 (readiness 只能由 sidecar 产生，TrustedRunner 只负责读取和验证，避免 worker 伪造或追加 readiness。)
- Q: Provider/Auth secret 是否允许进入 readiness、identity 或 evidence envelope？ → A：不允许；FD 3 只传一次性 raw secret，sidecar 读取后立即关闭；readiness/evidence 只记录 auth_ref、runtime/provider binding 和 contract digest，不记录 secret 或 secret hash。 (保持 AuthRuntime secret isolation：secret 只存在 sidecar 私有内存与 provider 受保护注入路径。)
- Q: Runtime sidecar 的复用粒度是什么？ → A：一个 sidecar 只服务一个 execution、一个 provider launch；与 execution process group 同生共死，FD 3/4 只消费一次，cleanup 或 terminal outcome 后退出并销毁 endpoint。 (当前单设备、单节点、单 Provider OPN Atom 采用 execution-scoped sidecar，避免跨 execution secret、registry snapshot 和状态污染。)
- Q: Human approval、admission 与 FD 3 secret 注入的关系是什么？ → A：FD 3 secret 只能在 Human approval 与 admission 完成、execution binding 冻结后由 TrustedRunner 注入；sidecar 不重新授权，worker/Agent 不能触发 secret 注入。 (Human/Human+Agent 中心责任单元保持唯一授权入口，Runtime sidecar 只执行已批准契约。)
- Q: 下一次实现采用哪种垂直切片？ → A：分三层连续推进：先实现 provider-neutral bootstrap envelope、状态机、FD transport 接口与内存 fixture；再接 macOS --pid/socket identity proof；最后接 ProviderAuth sidecar 的 FD 3/4/5 与统一 TrustedRunner process group。 (每层独立测试和 fail-closed 证据，第三层才进入真实 sidecar orchestration，降低故障定位范围。)
- Q: FD 4 binding 与 FD 5 readiness 是否使用同一套 schema？ → A：使用两个方向专用 schema：TrustedRunner→sidecar 的 identity binding schema，以及 sidecar→TrustedRunner 的 runtime readiness schema；两者都绑定 channel_role、execution context、correlation_id 和 binding_digest，不能互换。 (transport 只负责传输，协议层负责方向、语义和权限边界。)
- Q: bootstrap lifecycle 是否采用显式单向状态机？ → A：采用显式单向 bootstrap 状态机，状态为 starting → auth-ready → awaiting-peer-binding → runtime-ready → closed，任意阶段可进入 blocked/outcome-uncertain；不允许回退、重复或 terminal 后继续使用。 (bootstrap 状态机负责 sidecar readiness 与 channel 生命周期，不能用 status 字段替代顺序约束。)
- Q: bootstrap lifecycle 与 Provider/Execution lifecycle 如何分层？ → A：严格分离；bootstrap 只管理 starting/auth-ready/awaiting-peer-binding/runtime-ready/closed 或 blocked/outcome-uncertain，Provider/Agent execution/verification/cleanup 独立管理，通过 execution context、attempt、runtime binding 和 bootstrap digest 关联。 (runtime-ready 不代表 execution 成功；证据通过不可变 correlation/binding 关联，避免生命周期状态耦合。)
- Q: bootstrap 失败如何投影到 Execution lifecycle？ → A：按阶段投影：sidecar auth 失败且 worker 未启动为 Execution blocked；worker 已启动但有界清理成功仍为 blocked；无法证明 process-tree 完整终止为 outcome-uncertain；bootstrap 原始状态独立保留。 (Execution 只接收规则化 outcome projection，不能用 Execution 状态替代 bootstrap 状态，也不能掩盖残留进程风险。)
- Q: bootstrap evidence 是否必须进入 review handoff？ → A：必须作为独立 evidence set 进入 review handoff，包含 sidecar artifact/registry snapshot、process-boundary、PID/socket identity、FD channel lifecycle、state transitions、binding digests 和 cleanup proof；与 Provider result、Execution verification 分开。 (bootstrap 是 OPN execution 的独立证明链，Human review 必须能区分 bootstrap、Provider 启动、Execution 完成和结果验证。)
- Q: Human review 如何呈现 bootstrap evidence？ → A：以可读摘要 + 可校验 evidence references 呈现；默认显示 bootstrap 状态、identity/binding/process-boundary 结论与失败动作，详情通过 evidence ID、artifact path、canonical digest 和 registry snapshot ref 展开；secret、FD 内容和未脱敏 native stderr 不进入 review package。 (兼顾 Human 可读性、证据可追溯性与敏感材料隔离。)
- Q: bootstrap evidence 使用哪种存储和完整性模型？ → A：复用 content-addressed evidence store；proof artifact immutable 且按内容 digest 寻址，StateStore 只记录事件和 references，读取重新校验 network/execution/attempt/bootstrap digest；secret 与 FD 原始内容不入 store。 (StateStore 负责可查询生命周期，content-addressed store 负责脱敏 proof 完整性与长期引用。)
- Q: 第一层 provider-neutral protocol fixture 的测试范围是什么？ → A：覆盖完整 failure matrix：正常两阶段 readiness、FD 缺失/超时/截断/超长、重放/追加帧、状态乱序/重复/第三帧、schema/role 互换、digest/binding 漂移、terminal 后写入、secret 泄露，以及 blocked/outcome-uncertain 投影。 (协议状态机和 fail-closed 边界先独立闭合，避免 native integration 掩盖协议错误。)
- Q: sidecar 对 worker peer identity 的验证频率是什么？ → A：每次 Unix socket connection 都重新采集并验证 OS peer identity，再执行 challenge-response；不复用上一连接的 verified 状态。 (bootstrap expected identity 与 per-connection actual identity 必须同时成立，challenge nonce 不能替代 OS peer proof。)
- Q: execution-scoped sidecar 是否允许 worker 重连？ → A：每个 execution 只允许一个 verified worker connection；首次连接完成 peer identity + challenge 后绑定 execution/attempt，连接断开后不允许重连，worker restart/PID 变化/repeated launch 直接 blocked。 (与一次性 AuthRef、challenge、ProviderLaunchHandle 和 execution-scoped sidecar 保持一致，避免跨连接继续执行。)
- Q: sidecar 何时占用唯一 worker connection slot？ → A：第一个 connection 到达并通过 OS peer identity 后立即占用；随后进入 connection-reserved/challenge-verified/launch-accepted，任何 peer mismatch、challenge failure 或 timeout 都直接终止，slot 不释放、不允许第二个连接接管。 (避免错误 worker、攻击者或 stale endpoint 竞争首个 slot，execution 与首个真实 OS peer 一次性绑定。)
- Q: bootstrap failure reason 是否采用稳定 machine-readable taxonomy？ → A：采用稳定 reason codes 和可选 bounded 脱敏 detail；reason code 进入 StateStore、evidence、review handoff，并纳入协议测试矩阵；secret、native stderr 和敏感路径不直接进入 reason。 (支持 Human 可解释停下、机器路由、重放和 UI 映射，避免自由文本漂移。)
- Q: bootstrap reason taxonomy 放在哪里治理？ → A：纳入 provider-neutral protocol registry；reason code 固定 schema/version，声明 lifecycle stage、默认 outcome projection、Human retry/review policy，fixture/UI/evidence verifier 共用。 (reason taxonomy 是协议的一部分，不是分散在 sidecar、TrustedRunner 和 UI 中的日志字符串。)
- Q: Human retry 如何处理失败的 bootstrap？ → A：retry 创建全新的 execution attempt；原 sidecar、worker process group、FD 3/4/5、AuthRef/challenge/launch handle 均不复用，重新生成 execution/attempt、nonce、binding、socket，原失败 evidence 保留并关联。 (retry 是新的 execution，不是 failed bootstrap 的 in-place rebind；避免复用一次性授权和边界状态。)
- Q: retry 时 Human approval 是否重新签发？ → A：每个新的 execution attempt 都重新签发 Human approval；approval、AuthRef、secret、bootstrap binding、socket 和 process boundary 都 attempt-bound，不跨 retry 迁移。 (现有 approval contract 绑定 execution_id/attempt，重新签发保证 Human 明确确认新的执行上下文。)

**当前子树：**
├── [x][Y+] 1-4-1. Provider-neutral bootstrap protocol and memory failure-matrix fixture
├── [ ][Y+] 1-4-2. macOS process identity PID and Unix socket verification integration
└── [ ][Y+] 1-4-3. TrustedRunner Runtime sidecar FD bootstrap and execution process-group orchestration
<!-- ROADMAP_SECTION_END -->
