# Pattern Registry Is the Shared Pattern Index

The pattern registry is the machine-readable index for loop patterns, while individual pattern documents remain the human-facing explanation. We require new patterns to update `patterns/registry.yaml` because audit, cost estimation, docs, and scaffolding tools need one shared source for pattern metadata, even though maintaining the registry adds a small amount of duplication.
