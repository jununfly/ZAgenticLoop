# Grok — Dependency Sweeper Example

```bash
/loop 6h Run zj-dependency-triage for outdated and vulnerable packages. Update dependency-sweeper-state.md. Patch-only auto-fix in worktree with zj-loop-verifier. Escalate major bumps and denylisted packages.
```

See [patterns/dependency-sweeper.md](../../patterns/dependency-sweeper.md).