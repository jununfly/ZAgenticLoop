# zj-loop-generated: true
# zj-loop-template-id: github-actions/zj-loop-smoke
# zj-loop-template-version: 1
# zj-loop-template-hash: generated-by-zj-loop-init

name: ZJ Loop Smoke

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v6
        with:
          node-version: '22'

      - name: Smoke route decision
        run: |
          npx --yes --package @jununfly/zj-loop-core@0.1.6 zj-loop-route dispatch manual-smoke-report --json > route-decision.json
          npx --yes --package @jununfly/zj-loop-core@0.1.6 zj-loop-consumer plan manual-smoke-report --json > consumer-plan.json
          cat route-decision.json >> "$GITHUB_STEP_SUMMARY"
          cat consumer-plan.json >> "$GITHUB_STEP_SUMMARY"

      - name: Audit installed loop readiness
        run: npx @jununfly/zj-loop-audit@0.1.6 . --suggest
